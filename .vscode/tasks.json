{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "valgrind",
            "type": "shell",
            "command": "valgrind",
            "args": [
                "--leak-check=full",
                "${workspaceFolder}/minishell"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
